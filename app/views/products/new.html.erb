
<%= form_for(@product) do |p| %>
    <%= p.label :name %><br>
    <%= p.text_field :name %><br>
    <%= p.label :description %><br>
    <%= p.text_field :description %><br>
    <%= p.submit %>
<% end %>
